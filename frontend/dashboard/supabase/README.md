## Legacy Migration Folder

This folder is legacy and is not the source of truth for database migrations.

- Use root `supabase/migrations` for all new migrations.
- Do not apply migrations from `frontend/dashboard/supabase/migrations`.
- Existing files are kept only for historical context.
